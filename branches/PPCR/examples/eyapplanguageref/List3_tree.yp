%semantic token 'c' 
%tree
%{
use Data::Dumper;
$Data::Dumper::Indent = 1;
%}
%%
S:      'c'+  'd'+ 
           { 
              print Dumper($_[1]);
              print Dumper($_[2]);
           } 
;

%%

use base 'Tail';
__PACKAGE__->lexer(\&Tail::lex);
