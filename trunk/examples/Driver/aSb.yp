%lexer {
    s/^[ \t\n]*//;
    s/^(.)//s and return($1,$1);
}

%%
S:                 { print "S -> epsilon\n" }
    |   'a' S 'b'  { print "S -> a S b\n" }
;
%%

sub Run {
    my($self)=shift;

    $self->slurp_file('','Try aaabbb: ', "\n");
    $self->YYParse( 
       yydebug => 0x1F 
    );
}
