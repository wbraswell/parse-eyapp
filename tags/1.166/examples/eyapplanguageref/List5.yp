%semantic token 'c' 
%tree
%%
S: 'c' 'c'? 
     { 
       print $_[2]->str."\n";
       print $_[2]->child(0)->attr."\n" if $_[2]->children;
    }
;

%%
use base 'Tail';
__PACKAGE__->lexer(\&Tail::lex);

